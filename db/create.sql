CREATE TABLE leading_causes_of_death (
    year INT,
    year INT,
    leading_cause VARCHAR(100) NOT NULL,
    sex VARCHAR(1) NOT NULL,
    race_ethnicity VARCHAR(50) NOT NULL,
    deaths INT,
    death_rate INT,
    age_adjusted_death_rate INT
);
 
    
    
